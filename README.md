# PlaySound (Go Audio Library)

Легкая и гибкая библиотека на Go для воспроизведения аудиофайлов (MP3, WAV) из локальных источников или по протоколу **HTTPS**. Библиотека поддерживает зацикливание, управление громкостью и плавное затухание (fade-out).
## ✨ Особенности

* **Мультиформатность**: Поддержка MP3 и WAV.

* **Стриминг по HTTPS**: Плавное появление и затухание звука.

* **Эффекты Fade-In и Fade-Out**: Настройка громкости, зацикливания (loop) и плавного выключения.

* **Полный контроль**: ЗПауза, возобновление, изменение громкости и перемотка в реальном времени.

* **Авто-очистка**: Автоматическое удаление временных файлов после проигрывания.

## 📦 Установка
`Bash`

`go get github.com/Roman77St/playsound`

Вам также понадобятся системные библиотеки для работы с аудио (зависимости oto):

* Linux: libasound2-dev

* Windows/macOS: Дополнительные драйверы обычно не требуются.

## 🚀 Быстрый старт
**Простой запуск**

`Go`

```Go
package main

import "github.com/Roman77St/playsound"

func main() {
    // Проигрывание локального файла
    done, _ := playsound.PlaySound("ambient.mp3")
    <-done // Ожидание окончания
}
```

Продвинутое использование (HTTPS + Fade-out)

`Go`

```Go
package main

import (
    "time"
    "github.com/Roman77St/playsound"
)

func main() {
    params := playsound.PlayParams{
    Volume:   0.8,    // Громкость 80%
    Loop:     true,   // Бесконечный повтор
    FadeIn:   true,   // Плавный старт
    FadeOut:  true,   // Плавное затухание при остановке
    Position: 30,     // Начать с 30-й секунды
}

    // Воспроизведение по URL
    url := "https://example.com/music.wav"
    done, _ := playsound.PlaySoundWithParams(url, params)

    time.Sleep(10 * time.Second)

    // Остановить все звуки (сработает Fade-out)
    playsound.StopAll()

    <-done
}
```
## Динамическое управление

Вы можете управлять звуком, пока он играет, используя канал `done`:
`Go`
```Go
// Приостановить (с эффектом Fade-Out, если включен)
playsound.Pause(done)

// Продолжить (с эффектом Fade-In, если включен)
playsound.PlayOn(done)

// Изменить громкость "на лету"
playsound.SetVolume(done, 0.5)

// Перемотать на 10-ю секунду
playsound.Seek(done, 10)

// Узнать текущую позицию
pos, _ := playsound.GetPosition(done)
fmt.Printf("Сейчас играет: %d сек\n", pos)

// Остановить всё и очистить ресурсы
playsound.StopAll()

```
## Архитектура проекта

Библиотека разделена на логические модули для удобства поддержки:
* engine.go — Инициализация аудио-движка и глобальное состояние.
* decoders.go — Логика декодирования MP3/WAV и работа с временными файлами.
* controls.go — API для управления (Pause, Seek, Volume).
* monitor.go — Жизненный цикл звука и эффекты плавности.
* utils.go — Валидация параметров и математические расчеты.

## Тестирование

Библиотека покрыта тестами на логику состояний и математику.
`Bash`
```
go test -v -race ./...
```

## 📄 Лицензия

MIT License