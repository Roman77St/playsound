# PlaySound (Go Audio Library)

Легкая и гибкая библиотека на Go для воспроизведения аудиофайлов (MP3, WAV) из локальных источников или по протоколу **HTTPS**. Библиотека поддерживает зацикливание, управление громкостью и плавное затухание (fade-out).
## ✨ Особенности

* Мультиформатность: Поддержка MP3 и WAV.

* Стриминг по HTTPS: Возможность проигрывать файлы напрямую по ссылке.

* Умное моно: Автоматическое преобразование моно-файлов в стерео для предотвращения искажений звука.

* Параметры воспроизведения: Настройка громкости, зацикливания (loop) и плавного выключения.

* Плавное затухание (Fade-out): Звук не обрывается резко при остановке, а плавно затихает.

## 📦 Установка
Bash

go get github.com/Roman77St/playsound

Вам также понадобятся системные библиотеки для работы с аудио (зависимости oto):

* Linux: libasound2-dev

* Windows/macOS: Дополнительные драйверы обычно не требуются.

## 🚀 Быстрый старт
**Простой запуск**

`Go`

```Go
package main

import "github.com/Roman77St/playsound/play"

func main() {
    // Проигрывание локального файла
    done, _ := play.PlaySound("ambient.mp3")
    <-done // Ожидание окончания
}
```

Продвинутое использование (HTTPS + Fade-out)

`Go`

```Go
package main

import (
    "time"
    "github.com/Roman77St/playsound/play"
)

func main() {
    params := play.PlayParams{
        Volume:  0.2,   // Громкость 20%
        Loop:    true,  // Бесконечный цикл
        FadeOut: true,  // Плавное затухание при остановке
    }

    // Воспроизведение по URL
    url := "https://example.com/music.wav"
    done, _ := play.PlaySoundWithParams(url, params)

    time.Sleep(10 * time.Second)

    // Остановить все звуки (сработает Fade-out)
    play.StopAll()

    <-done
}
```
## 🛠 Технические детали

* Движок: Использует ebitengine/oto/v3 для низкоуровневой работы с аудио.

* Декодеры: go-mp3 и youpy/go-wav.

* Память: При работе с HTTPS файлы временно загружаются в память для обеспечения возможности перемотки (Seek), необходимой для корректного зацикливания.

## 📄 Лицензия

MIT License